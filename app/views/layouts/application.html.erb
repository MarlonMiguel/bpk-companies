<!DOCTYPE html>
<html>
  <head>
    <title>Biopark Empresas</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= stylesheet_link_tag "custom", "data-turbo-track": "reload" %>
  
    <% if content_for?(:header) %>
      <%= yield :header %> <!-- Estilos específicos por página -->
    <% end %>

    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Renderiza mensagens de flash -->
    <%= render 'shared/flash_messages' %> 

    <div class="d-flex justify-content-end" style="background-color: transparent; padding: 0; margin: 0; position: absolute; top: 10px; right: 10px; z-index: 1000;">
      <% unless current_page?(root_path) %>
        <%= link_to content_tag(:i, '', class: 'bi bi-house-door-fill btn btn-sm btn-secondary me-2'), root_path, class: 'nav-link' %>
      <% end %>
      
      <% if user_signed_in? %>
        <%= button_to 'Logout', destroy_user_session_path(locale: I18n.locale), method: :delete, data: { confirm: 'Tem certeza que deseja sair?' }, class: 'btn btn-sm btn-danger' %>
        
        <% if current_user.profile_image.attached? %>
          <%= link_to image_tag(current_user.profile_image.variant(resize_to_limit: [30, 30]), class: "rounded-circle", style: "width: 35px; height: 35px; object-fit: cover; margin-left: 10px;"), edit_user_registration_path %>
        <% else %>
          <%= link_to image_tag('default_profile_image.png', class: "rounded-circle", style: "width: 25px; height: 25px; object-fit: cover; margin-left: 10px;"), edit_user_registration_path %>
        <% end %>
      <% end %>
    </div>

    <!-- Renderiza o conteúdo principal da view (ex: carousel) -->
    <%= yield %>
    <!-- Rodapé fixo -->
    <footer class="footer custom-footer">
      <div class="container-fluid">
        <div class="row">
          <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
              <div class="text-start">
                <span class="text-white">&copy; <%= Time.now.year %> Marlon Miguel. Contato: marlonmiguel1993@gmail.com | (45) 999067104</span>
              </div>
              <div class="text-end">
                <div class="language-select">
                  <%= link_to image_tag('country/en.png', alt: 'English', class: 'language-flag'), url_for(locale: 'en') %>
                  <%= link_to image_tag('country/pt.png', alt: 'Portuguese', class: 'language-flag'), url_for(locale: 'pt') %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </footer>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/2.11.8/umd/popper.min.js"></script>
  </body>
</html>